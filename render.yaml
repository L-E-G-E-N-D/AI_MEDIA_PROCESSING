services:
  - type: web
    name: ai-media-service
    runtime: docker
    region: oregon
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "3000"
      - key: REDIS_URL
        value: "redis://127.0.0.1:6379"
      - key: DATABASE_URL
        sync: false
